{
  "name": "prionsenligne-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Scripts de génération audio TTS pour le chapelet (Google Cloud TTS via REST API)",
  "scripts": {
    "extract": "node extract-texts.js",
    "generate": "node generate-tts.js",
    "all": "node extract-texts.js && node generate-tts.js"
  },
  "engines": {
    "node": ">=18"
  }
}
